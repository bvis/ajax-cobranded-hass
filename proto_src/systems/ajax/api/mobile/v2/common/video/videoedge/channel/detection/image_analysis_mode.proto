syntax = "proto3";

package systems.ajax.api.mobile.v2.common.video.videoedge.channel.detection;

option java_multiple_files = true;
option swift_prefix = "";

enum ImageAnalysisMode {
  IMAGE_ANALYSIS_MODE_UNSPECIFIED = 0;
  IMAGE_ANALYSIS_MODE_BY_FRAME = 1;
  IMAGE_ANALYSIS_MODE_BY_CAMERA_ALGORITHM = 2;
}
